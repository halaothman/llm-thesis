#  نظام توليد الأسئلة التعليمية ودراسة أثر التوليد المعزز بالاسترجاع

## التشغيل

```bash
pip install -r requirements.txt
```

```bash
ollama pull llama3.2:3b
ollama pull qwen2.5:7b
```

```bash
streamlit run app.py
```

يفتح المتصفح على `http://localhost:8501`
